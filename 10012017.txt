//almost nothing new
//functions
//слогов столько, сколько же гласных
//получаем кол-во слогов, возвращает слова с таким же кол-вом слогов
//все в ф:
1)открытие, чтение, унификация+очистка(опустить заглавные, убрать знаки препинания), возврат массива
2)принимает число, возвращает слова с таким же кол-во слогов
доп.:
3)принимает букву, возвращает слова, которые начинаются с этой буквы
для этого
4)пользователь выбирает, что хочет получить 2) или 3) на выводе
см. задачу еее.ру

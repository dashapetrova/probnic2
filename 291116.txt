python.org
работа с текстами
script.py and 1.txt
f=open("1.txt") - greeting
print(f) - doesn't work in this way; in f no text1
points matter: name of the doc(1.txt) - in the same place or another one
пути:абсолютный(из любой в любую точку, конкретная точка), относительный

абсолютный: //разделители  \-символ экранирования, отменяет значение. Например, \n - не два символа, а один(n уже не n)
так может случиться с ледующем случае:
C:\Users\student\1.txt - так не найдёт
варианты решения: 1)экранировать экранирование(\\) 2) обмануть винду (/)
3) оставить обратный слеш, но сказать, чтобы не обращал внимания питон(r"C:\..." # or P ???)

относительный: относительно программы(есть программа, есть питоновский интерпретатор)
в той же папке что и программа - f=open("1.txt")
в папке где папка - f=open("folder/1.txt")

путь можно сделать независимым от ОС
продлемы с кодировками(unicode decode error) utf-8   cp1251(code page - третий питон юзает по умолчанию)
f=open("1.txt", 'r', encoding="utf-8") - неименованные плюшки
Notepad++ - узнать про кодировку
point matter: f != file (так называть нельзя, так как есть функция file)

Чтение файла: 
можно записать в переменную весь текст - s=f.read()
a=s.split('\n') - массив из строк текста or s=f.read().split('\n')
or a=f.readlines() - не может быть продоллжением s=f.read - читаем только один раз при одном открытии
for line in f: print(line) - аналайзим строки или дробим строку: от начала строки до \n
два переноса строки будет от print and line

i=0
for line in f:
print(line)
i+=1 - подсчет строк - или a=f.readlines() print(len(a))

i=0
for line in f: words=line.split()
i+=len(words)
print(i) - подсчет слов

Leaving:f.close()

задача:вывести все строки, которые начинаются с гласной
